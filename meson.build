project('contact-manager', 'cpp',
  version : '1.0',
  default_options : ['warning_level=3',
                     'cpp_std=c++14'])

exe = executable('contact_manager', 'contact_manager.cpp',
  install : true)

test('basic', exe)
